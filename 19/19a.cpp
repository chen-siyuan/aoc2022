#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {
    ifstream fin("../19/19.txt");

    clock_t timer = clock();
    cout << fixed << setprecision(6) << double(clock() - timer) / CLOCKS_PER_SEC << '\n';
}